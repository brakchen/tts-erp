"""tts_erp_v2.static — design-token CSS + page JS for the operator console.

Mounted by ``tts_erp_v2.app:build_app()`` via ``StaticFiles(directory=...)``
under the ``/static/`` URL prefix.

Only files in this directory are served — sub-package layout matches the
URL path: ``/static/css/console.css`` → ``tts_erp_v2/static/css/console.css``,
``/static/js/console.js`` → ``tts_erp_v2/static/js/console.js``.

No Python module of substance lives here; the directory exists so the
``StaticFiles`` mount has a stable target during tests.
"""
