"""analytics_sync service package.

A standalone FastAPI backend that replaces the Chrome extension's
retired CloudBase analytics upload path. See schema.sql for the
database tables, app.py for the FastAPI application, and
analytics_sync_tokens.py for the operator CLI.
"""
